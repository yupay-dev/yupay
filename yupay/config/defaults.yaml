# Configuración global por defecto
seed: 42
locale: "es_PE"

defaults:
  null_rate: 0.05
  duplicate_rate: 0.01

naming:
  customers:
    source: "locales.yaml"
    weighted: true
  products:
    source: "catalog.yaml"

generation:
  rows: 10000
  format: "parquet" # options: csv, parquet, duckdb
  output_root: "./data"
